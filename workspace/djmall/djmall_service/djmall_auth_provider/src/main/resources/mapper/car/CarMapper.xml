<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 
	namespace:用于隔离SQL全局唯一（采用接口和XML开发时为接口的全路径）
 -->
<mapper namespace="com.dj.mall.auth.pro.mapper.carshop.CarMapper">

    <select id="findCarById" resultType="com.dj.mall.auth.pro.bo.CarBO">
        select * from djmall_shop_car sc
        left join djmall_mall_product dmp on sc.product_id = dmp.id
        left join djmall_product_sku dps on sc.sku_id = dps.id
        where sc.user_id = #{id} and sc.check_status = 1
    </select>
</mapper>