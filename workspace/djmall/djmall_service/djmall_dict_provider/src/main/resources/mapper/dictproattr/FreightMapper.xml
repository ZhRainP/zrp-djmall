<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 
	namespace:用于隔离SQL全局唯一（采用接口和XML开发时为接口的全路径）
 -->
<mapper namespace="com.dj.mall.dict.pro.mapper.freight.FreightMapper">

    <select id="list" resultType="com.dj.mall.dict.pro.entity.FreightEntity">
        select df.id, bd.dictionary_name as logistics_company, df.freight as freight
        From djmall_dict_freight df
        left join dimall_bash_dictionary bd on df.logistics_company = bd.code
    </select>
</mapper>