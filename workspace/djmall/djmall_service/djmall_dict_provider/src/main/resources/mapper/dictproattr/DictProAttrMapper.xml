<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 
	namespace:用于隔离SQL全局唯一（采用接口和XML开发时为接口的全路径）
 -->
<mapper namespace="com.dj.mall.dict.pro.mapper.dictproattr.DictProAttrMapper">

    <select id="proAttrList" resultType="com.dj.mall.dict.pro.bo.DictProAttrBO">
        SELECT
            dpa.*, GROUP_CONCAT(dpav.attr_value) as attr_value
        FROM
            djmall_dict_product_attr dpa
        LEFT JOIN djmall_dict_product_attr_value dpav on dpa.id = dpav.attr_id
        GROUP BY dpa.id
    </select>
</mapper>